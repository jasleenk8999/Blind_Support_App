name: smart_vision_stick
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_blue_plus:
  google_maps_flutter:
  geolocator:
  flutter_tts:
  speech_to_text:
  vibration:
  audioplayers:
  firebase_core:
  cloud_firestore:
  firebase_auth:
  url_launcher:
  http:
  flutter_polyline_points:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/audio/alert.mp3
